#include <iostream>

using namespace std;

#define endl '\n'
int W, H, f, c, x1, y1, x2, y2;
void input();
void solve();

int main() {
  ios::sync_with_stdio(false);
  cin.tie(NULL);
  cout.tie(NULL);

  input();
  solve();

  return 0;
}

void input() { cin >> W >> H >> f >> c >> x1 >> y1 >> x2 >> y2; }

void solve() {
  
}
